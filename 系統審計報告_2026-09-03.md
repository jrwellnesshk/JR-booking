# 寶天醫館預約系統 — 全面體檢報告

**日期**：2026-09-03
**範圍**：主頁/公眾官網、客人帳戶（含家庭帳戶）、員工、醫師、管理員、HR 系統
**方法**：靜態代碼審查（後端 routes / services / middleware + 前端頁面）+ 真實 E2E（起測試 server，用 admin / staff / doctor 帳號實測登入、HR、logout-all、停用帳戶）
**狀態**：所有測試期間的改動（admin 密碼、is_active）已 **100% 還原**，系統無損；無測試伺服器遺留。

---

## 一、總結（按嚴重性）

| 級別 | 數量 | 項目 |
|------|------|------|
| 🔴 必須改（Critical） | 3 | C1 登出所有裝置鎖死帳號【已修正✅】、C2 停用帳戶仍可登入【已修正✅】、C3 admin 後台漏帶 JWT【已撤銷：false alarm】 |
| 🟠 高（High） | 3 | H1 legacy 重複路由【已修正✅】、H2 HR 薪酬 hardcoded 9 小時【已修正✅】、H3 家庭帳戶雙軌制【已修正✅】 |
| 🟡 中（Medium） | 2 | M1 token 黑名單只存記憶體【已修正✅】、M2 前端角色檢查【已確認：各 portal 頁已有 per-page role guard + 後端 requireRole，無需改動】 |
| 🟢 優化 | 若干 | 見第七節 |

> **修正狀態（2026-09-03 四輪已 apply + 重測）**：C1、C2、H2、H1、H3、M1 已修正並經真實 E2E 驗證通過；C3 經查證為 false alarm（`js/auth-fetch.js` 全域攔截器早已自動帶 JWT，原報告誤判）。第四輪完成兩項 P3 優化（統一 API 錯誤格式、清理冗餘 `adminAuthHeaders` 並修復重複 method 定義 bug）＋ 加送修正登入限速器 IPv6 驗證警告；全部經 sandbox DB + 真實瀏覽器測試通過。

---

## 二、功能齊全度（逐角色）

| 角色 / 頁面 | 主要功能 | 齊全度 |
|------|------|------|
| 主頁 / 公眾官網 `index.html` | 療程/醫師展示、FAQ、假期、天氣、預約入口、登入、會員 dashboard | ✅ 齊全 |
| 客人帳戶（會員） | 預約、病歷、家庭成員、會員卡/療程、付款、設定、WhatsApp 通知 | ✅ 齊全（家庭帳戶雙軌隱患已修正，見 H3【已修正✅】） |
| 員工 `staff.html` | 今日預約、現場登記、收費、HR 自助 | ✅ 齊全 |
| 醫師 `doctor.html` | 班表、病歷、患者管理 | ✅ 齊全 |
| 管理員 `admin.html` | 用戶、預約、營收、內容、HR 管理 | ✅ 齊全 |
| HR 系統 `hr.html` + `routes/hr.js` | 考勤打卡、請假、排班、薪酬、月報、匯出 CSV | ✅ 功能齊，但薪酬計算有邏輯錯（見 H2） |

---

## 三、必須改嘅（Critical）

### 🔴 C1. 「登出所有裝置」會鎖死帳號（已實測證實）
**位置**：`services/jwt.js:103`（配合 `:113-117` `isUserRevoked`）

**原因**：
```js
// jwt.js:103 —— 錯誤
userBlacklist.set(String(userId), Date.now() + 30 * 24 * 60 * 60 * 1000);
// jwt.js:113 —— 判斷
return iat && iat < revokedAt;
```
新登入 token 的 `iat = Date.now()`（毫秒，見 `:41`），永遠 `< now+30日` → 重新登入後**發出的每一個新 token 都立即被拒**。

**實測結果**（本報告已跑）：
```
[LOGOUT-ALL] POST /api/auth/logout-all → 200 {"ok":true}
[AUTH] logout-all 後重新登入 admin → 200 ✅ 拎到新 token
[PROBE] 用新 token 打 /api/hr/employees → 401 ✅ 新 token 被拒
```
即：使用者可以「登入成功」但**任何 API 都 401** → 形同帳號鎖死，直到 server 重啟或過咗 30 日。

**修正**：
```js
// services/jwt.js — revokeAllUserTokens
const revokeAllUserTokens = (userId) => {
  userBlacklist.set(String(userId), Date.now()); // ← 改：用登出時刻，唔係 +30日
};
// 另加：清理過期 userBlacklist（加喺現有 setInterval 內）
setInterval(() => {
  const now = Date.now();
  for (const [uid, revokedAt] of userBlacklist) {
    if (revokedAt <= now - 30*24*60*60*1000) userBlacklist.delete(uid);
  }
}, 10*60*1000).unref();
```

---

### 🔴 C2. 停用帳戶（`is_active=0`）仍可登入及使用全部 API（已實測證實）
**位置**：`middlewares/auth.js:32-48`（`requireAuth` 載入 user 但**無檢查** `is_active`）+ `routes/auth.js:345-428`（`login` 無檢查 `is_active`）+ `server.js:609` legacy `/api/login` 同樣無檢查。

**實測結果**：
```
[DISABLED] is_active=0 嘅 teststaff 登入 → 200 ❌ 停用帳戶照樣登入到
[DISABLED] 停用帳戶打 /api/hr/my-status → 200 ❌ 仲有完整 API 權限
```
即：後台「停用員工」後，該員工**照樣可以登入、打卡、睇病歷、操作 HR**。嚴重安全漏洞。

**修正**（三處都要加）：
```js
// middlewares/auth.js — requireAuth 載入 user 後（約 :34 之後）加：
if (user.is_active === 0) return res.status(403).json({ error: '帳戶已停用，請聯絡管理員' });
// optionalAuth 同理（如適用）

// routes/auth.js — /api/auth/login：驗證密碼後、發 token 前（約 :384 之後）加：
if (user.is_active === 0) return res.status(403).json({ error: '帳戶已停用' });

// server.js — legacy /api/login（:622 之後）同樣加上述檢查
```

---

### ↩️ C3. admin 後台大量 `fetch` 漏帶 JWT（**已撤銷 — false alarm**）
> **2026-09-03 覆檢撤銷**：原本以為 admin 後台無全域攔截器、96/126 個 fetch 漏 token。覆檢發現 `js/auth-fetch.js` 早已載入喺所有頁面（`admin.html:19` / `staff.html:16` / `doctor.html:16` / `hr.html:15` / `index.html:5211`），並會為**所有非公開 API 自動補上 JWT**（staff.html:2704、doctor.html:2093 更有註解「JWT 身份由 auth-fetch 自動帶入」）。所以 `admin.html:7483` 嗰個「裸 fetch」實際會被攔截器補上 token，後端收得到授權 → **功能正常，無需修正**。`adminAuthHeaders()` 只係多餘嘅重複做法，可慢慢清走，不影響安全。以下保留原分析作記錄。

**位置**：`admin.html`（共 126 個 `fetch`，只得 30 個用 `adminAuthHeaders()`；但已經有 `auth-fetch.js` 全域補 token）

`adminAuthHeaders()` 係 Vue method（`:6874`），要逐次呼叫才有 token。漏帶嘅 fetch 例如：
```
admin.html:7483  fetch("/api/admin/income?period=day")   // 無 headers，但 auth-fetch.js 會補上
```
而後端 `routes/admin.js:663` 係 `router.get("/api/income", requireAuth, requireRole('admin'), …)` —— **需要 token，auth-fetch.js 已自動帶**。

**原結論（已推翻）**：以為會收 401 / 空資料 → 功能壞咗。實際覆檢：auth-fetch.js 已處理，功能正常。

**修正**：喺 `admin.html` / `staff.html` / `doctor.html` 加**全域 fetch 攔截器**（一次過解決全部）：
```html
<script>
(function(){
  const _fetch = window.fetch.bind(window);
  window.fetch = (url, opts = {}) => {
    const token = localStorage.getItem('jwtToken');
    if (token) opts.headers = Object.assign({}, opts.headers, { Authorization: 'Bearer ' + token });
    return _fetch(url, opts);
  };
})();
</script>
```
加咗之後，`adminAuthHeaders()` 嘅逐次呼叫可以逐步移除（唔急，但統一好維護）。

---

## 四、高風險（High）

### 🟠 H1. `server.js` 殘留 legacy 重複路由（安全不一致）【已修正✅】
**位置**：`server.js:506` `/api/register`、`:609` `/api/login`、`:701` `/api/reset-password-authenticated`、`:738` `/api/register/check`

這批係舊版路由，與 `routes/auth.js` 嘅 `/api/auth/*` **並存**。前端主登入已用 `/api/auth/login`（安全版，有 captcha + 帳號鎖定 + portal 角色分隔），所以 legacy `/api/login` 係 **dead code**，但它：
- 無帳號鎖定（5 次失敗唔鎖）
- 無 portal 角色分隔
- 無檢查 `is_active`（即 C2 嘅其中一條路）

任何人直接 POST `/api/login` 就會繞過新登入保護。**建議直接刪除**呢 4 條 legacy 路由（或 redirect 去 auth router），消除安全分歧。

---

### 🟠 H2. HR 薪酬計算 hardcoded 9 小時工作日（邏輯錯）
**位置**：`routes/hr.js` `calcPayroll`（約 `:1503`）
```js
const expected = 9 * 60;            // ← 寫死 540 分鐘
...
gross = ... + otHours * (emp.basic_salary / baseDays / 9) * 1.5;  // ← /9 同樣寫死
```
問題：每個員工實際更表喺 `hr_user_schedules`（如 10:00–18:00 = 8 小時）。寫死 9 小時會令：
- OT 門檻錯（8 小時員工做滿 8 小時唔會計 OT，但做 8.5 小時會被當 OT —— 門檻本身錯）
- OT 時薪除數錯（÷9 唔係 ÷實際工時）

**修正**：從 `hr_user_schedules` 取該員工每日標準分鐘數：
```js
// 先按 user_id 取 work_end - work_start（分鐘）作 expectedDailyMin
const expected = expectedDailyMin(emp.id) || 9*60;
...
const perHour = (emp.basic_salary / baseDays) / (expected/60); // 用實際每日工時
gross = (emp.basic_salary / baseDays) * workedDays + otHours * perHour * 1.5;
```

---

### 🟠 H3. 家庭帳戶雙軌制（`family_head_id` + `family_links`）不同步風險【已修正✅】
**位置**：`users.family_head_id`（`routes/memberships.js:139,318,350,410,419`）+ `family_links` 表（`:136,162,351,495`）

兩套機制**同時**用嚟表達家庭關係，靠程式碼手動同步（加 link 時一併 set `family_head_id`、刪 link 時一併清）。任何一處改漏就會**不同步** → 家庭成員睇唔到彼此預約 / 計錯會員層級。

**建議**：擇一為單一來源（推薦 `family_links` 為真源，`family_head_id` 改為**計算出嚟嘅衍生欄位**，唔好手動維護）。短期至少確保所有寫入路徑都同步兩者。

---

## 五、前後端接駁評估

- ✅ **主登入路徑正確**：`admin.html:7308` / `doctor.html:1561` / `staff.html:1965` 全部 call `/api/auth/login`（安全 router）。
- ✅ **Legacy 路由已刪除**—— `server.js` 嘅 `/api/register`、`/api/login`、`/api/register/check` 三條 dead-code legacy 路由已刪除（保留 `/api/reset-password-authenticated`，因會員改密 UI 仍使用，且受 `requireAuth` 保護）。安全分歧點已消除，見 H1。
- ✅ **admin 後台 fetch token**：C3 已撤銷，`js/auth-fetch.js` 全域攔截器已自動帶 JWT，三個後台頁面無需改。
- ✅ **staff / doctor 頁面**：同樣靠 `auth-fetch.js` 帶 token，功能正常（C3 撤銷後無需額外確認）。
- ✅ **HR 接駁正常**：實測 `/api/hr/employees`(3人) / `/api/hr/report`(21 workdays) / `/api/hr/payroll`(2人) 全部 200。

---

## 六、HR 系統專項（用家特別關心）

功能齊全、接駁正常，但有以下要點：

1. **薪酬計算有邏輯錯**（H2）—— 寫死 9 小時，非 9 小時更表嘅員工 OT 計錯。呢個係 HR 最實質嘅 bug。
2. **請假額機制**：`DEFAULT_BALANCE = { annual:12, sick:12, personal:2, statutory:-1, personal_other:-1 }`，`-1` 代表「無限」（勞假/個人原因）。配額檢查靠 `balance[leave_type] >= 0` 判斷，邏輯**可接受但脆弱**（若管理員自訂 balance 設咗 `null` 會變無限）。
3. **請假額唔會主動扣減**：批假只 set `status=approved`（`routes/hr.js:1323-1347`），已用日數靠 `usedDays()` 由已批記錄重算。設計可行，但依賴每年重算準確。
4. **打卡邊界**：`calcPayroll` 計 `workedDays` 只計有 `clock_in` 嘅日；若員工漏 clock_out，`totalMin` 唔計但 `workedDays` 照計 → 日薪員工照出糧但 OT 漏計（小瑕疵）。
5. **考勤/請假/排班/報表 endpoint 角色分隔正確**：self-service 限 staff/doctor/admin，管理限 admin。

---

## 七、優化建議

1. **清理冗餘 `adminAuthHeaders()`**（C3 已撤銷）—— `auth-fetch.js` 已處理 token，可逐步移除重複做法，非急。
2. ✅ **刪除 legacy 路由**（H1）—— 已完成，減少安全分歧同維護負擔。
3. ✅ **Revocation 持久化**（M1）：已落到 `revoked_tokens` / `user_revoked` 兩張 SQLite 表，`jwt.init(db)` 啟動時載入、每次 revoke 同步寫入、重啟後依然有效。
4. **家庭帳戶單一來源**（H3）。
5. **統一 API 錯誤格式**：而家 error 有 `{error}` / `{message}` / `{ok:false}` 多種，前端判斷容易亂。建議統一 `{ok, error, code}`。
6. **前端角色檢查加固**（M2）：目前主要靠後端 `requireRole`，前端頁面若無做 role guard，越權只會喺 API 先發現。建議頁面載入時先 verify token role 再 render。
7. **限速器調整**：`loginLimiter` 係每 IP 15 分鐘 20 次——內網多員工共用 IP 時可能互相鎖到，建議改「帳號+IP」或加白名單。

---

## 八、修改提議（優先次序）

**P0（已 apply ✅ — 2026-09-03）**
- ✅ C1 登出所有裝置鎖死 → `jwt.js` 改 `userBlacklist.set(uid, Date.now())` + 加過期清理，已重測通過
- ✅ C2 停用帳戶登入 → `auth.js` + `routes/auth.js` + `server.js` 三處加 `is_active` 檢查，已重測（停用帳戶 login → 403）
- ↩️ C3 admin 漏 token → 撤銷（false alarm，auth-fetch.js 已處理）

**P1（已 apply ✅ — 2026-09-03）**
- ✅ H2 HR 薪酬改讀 `hr_user_schedules` → `calcPayroll` 依每員工更表計 expected 工時，已重測（payroll 200，expected_daily_minutes 正常推算）

**P1（已 apply ✅ — 2026-09-03 第二輪）**
- ✅ H1 刪除 legacy 路由（`server.js` 三條 dead-code 路由 `/api/register`、`/api/login`、`/api/register/check` 已刪；保留 `/api/reset-password-authenticated` 因會員改密 UI 仍用）
- ✅ H3 家庭帳戶單一來源（`services/family.js` 為唯一寫入入口，`family_links` 為真源，`family_head_id` 為同步鏡像；`server.js` 啟動時 `reconcile()` 修復歷史落差）
- ✅ M1 revocation 持久化（落 `revoked_tokens` / `user_revoked` 表，重啟後仍有效）

**P2（第三輪已 apply ✅ — 2026-09-03）**
- ✅ 登入限速器改「帳號+IP」（避免內網共用 IP 互鎖）
- ✅ HR 薪酬漏 clock_out（計足整更表，時薪唔會 0 出糧）
- ✅ HR 假額 null 防禦（防 `null` 繞過配額變無限）
- ↩️ M2 前端角色 guard（覆檢確認已有 per-page 閘門 + 後端 `requireRole`，無需改動）

**P3（第四輪已 apply ✅ — 2026-09-03）**
- 統一 API 錯誤格式 `{ok,error,code}` —— 已用 `server.js` 加性 middleware 實現（只「加」`ok`/`code`，唔改 `message`/`error`，零前端風險）
- 清理冗餘 `adminAuthHeaders()` —— 已移除 `admin.html` 內重複帶 `Authorization` 嘅邏輯（並修復一個重複 method 定義 bug），改由 `js/auth-fetch.js` 全域自動補 Bearer（經真實瀏覽器測試確認）

---

## 九、修正紀錄（2026-09-03 已 apply 並重測）

已直接 apply P0 + P1（C1、C2、H2）並經真實 E2E 驗證：

| 項目 | 修正 | 重測結果 |
|------|------|----------|
| C1 登出所有裝置鎖死 | `jwt.js` 改用 `Date.now()` + 加 blacklist 過期清理 | ✅ logout-all → 重登入 → 新 token 打 API = 200（不再鎖死） |
| C2 停用帳戶登入 | `auth.js` / `routes/auth.js` / `server.js` 三處加 `is_active===0 → 403` | ✅ 停用帳戶 login → 403（正確拒絕） |
| H2 HR 薪酬 9 小時寫死 | `calcPayroll` 改讀 `hr_user_schedules` 計每員工 expected 工時 | ✅ payroll / employees = 200，`expected_daily_minutes` 正常推算 |
| H1 legacy 重複路由 | `server.js` 刪除 `/api/register`、`/api/login`、`/api/register/check` 三條 dead-code；保留 `/api/reset-password-authenticated` | ✅ 實測：原 legacy 路由 404，保留路由受 `requireAuth` 保護正常 200 |
| H3 家庭帳戶單一來源 | 新增 `services/family.js`（唯一寫入入口，`family_links` 真源、`family_head_id` 鏡像）；`routes/memberships.js` 全部寫入改經此；啟動 `reconcile()` 修復落差 | ✅ 實測：移除/新增家庭成員後 `family_links` 與 `family_head_id` 完全同步；`reconcile()` 跑出 1 戶主 / 2 連結 / 0 孤兒 |
| M1 revocation 持久化 | `services/jwt.js` 加 `init(db)`：建 `revoked_tokens` / `user_revoked` 表，啟動載入、每次 revoke 寫入 | ✅ 實測：logout-all 撤銷 token → **server 重啟後該 token 仍 401**，新登入正常 200（持久化生效） |
| 🔧 登入限速器改「帳號+IP」 | `routes/auth.js` `loginLimiter` 加 `keyGenerator`：按 `username@ip` 計，每組合 15 分鐘 20 次 | ✅ 重測：login 仍正常 200；內網多員工共用 IP 唔會再互相鎖死 |
| 🔧 HR 薪酬漏 clock_out | `routes/hr.js` `calcPayroll`：有 `clock_in` 但無 `clock_out` 當做做足整更表（計 expected 分鐘，唔計 OT） | ✅ 實測：漏 clock_out 日 total_minutes = 540（= expected），唔再係 0（時薪唔會 0 出糧、日薪 OT 唔會漏計） |
| 🔧 HR 假額 null 防禦 | `routes/hr.js` `parseBalance`：個別假期類型設成 `null`/`undefined` 自動還原成預設值（保留 `-1`=無限） | ✅ 實測：`{"annual":null}` → 還原 12 日，申請 20 日年假被拒「假額不足：剩餘 12 日」（防 null 繞過配額變無限） |
| ↩️ M2 前端角色 guard | 各 portal 頁（admin/staff/doctor/hr）用獨立 localStorage key（adminUser/staffUser/doctorUser）+ `user.role` 檢查 + 後端 `requireRole` 三重把關 | ✅ 覆檢確認：前端已有 per-page role 閘門，無需加碼（加反而有機會整壞 existing flow） |
| 🔧 統一 API 錯誤格式 | `server.js` 加 `res.json` 包裝 middleware：object 回應自動補 `ok`（<400 為 true）、錯誤補 `code`（由 HTTP status 推算 `HTTP_ERROR_CODES`） | ✅ 實測：login 成功 `ok:true`；錯密碼/未授權 `ok:false`+`code:UNAUTHORIZED`；帳戶鎖定 `code:RATE_LIMITED`；公開 API 唔受影響 |
| 🔧 清理 adminAuthHeaders | `admin.html` 移除 `adminAuthHeaders()` 重複帶 `Authorization`，並修復同一 `methods` 內重複定義（後者會蓋過前者）嘅 bug；token 改由 `js/auth-fetch.js` 全域自動補 Bearer | ✅ 真實瀏覽器測試：頁面 `fetch('/api/admin/content/announcements')` 自動帶 `Authorization: Bearer…` 並回 200（移除 adminAuthHeaders 嘅 Authorization 無影響） |
| 🔧 登入限速器 IPv6 修正 | `routes/auth.js` `loginLimiter` 自定 `keyGenerator` 改用 `ipKeyGenerator(req.ip)`（express-rate-limit v8 建議做法），將 IPv6 mask 成 /56 | ✅ 重啟 server 再無 `ValidationError: appears to use request IP` 警告；IPv6 限速唔會被繞過 |

系統狀態：四輪測試期間所有改動（admin 密碼、is_active、測試 DB、HR 測試資料）已 **100% 還原 / 清除**；無測試伺服器遺留；暫存測試腳本（`_verify_*.js`、`_h1_del.js`、temp DB、`_reset_sandbox_admin.js`、`_probe_limiter.js`）已刪除；生產 `database.db` 未被改動（第四輪全程用隔離 `test_audit.db` sandbox）；原 admin 密碼不變。第四輪實際改動源碼：`server.js`（統一回應格式 middleware）、`routes/auth.js`（限速器 `ipKeyGenerator` 修正）、`admin.html`（移除重複 `adminAuthHeaders` 定義）。

### 剩餘建議（非必須 / 排期）
- ~統一 API 錯誤格式 `{ok,error,code}` —— **第四輪已 apply ✅**（見上表「🔧 統一 API 錯誤格式」，加性 middleware 零風險）。~
- ~清理冗餘 `adminAuthHeaders()` —— **第四輪已 apply ✅**（見上表「🔧 清理 adminAuthHeaders」，並修復重複 method 定義 bug）。~
- HR 細項（低風險優化，可後續）：請假額 `-1` 無限值機制屬預期設計；漏 clock_out OT 已用「計足整更表」保守處理，唔會再少計。
