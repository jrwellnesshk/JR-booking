# 寶天醫館預約系統 — 30 客全流程場景測試報告

**測試日期**：2026-08-24（凌晨）
**測試環境**：本機 `http://localhost:4000`，Node v22.17.0，SQLite `database.db`
**模擬規模**：一日 **30 位客人**完整生命週期（對應你每月約 400 客嘅高峯日）
**測試方式**：自動化 E2E 腳本直接調用 API（`_e2e_scenario_test.js`），共 **237 個 API 調用 / 50 秒**
**總結果**：**180 通過 / 1 項例外**（事後專項驗證證實為系統 bug，見 🔴-3）

---

## 一、30 客場景設計

| 組別 | 人數 | 模擬情境 |
|---|---|---|
| 訪客 V1–V3 | 3 | 無帳戶網上預約初體驗（S1） |
| 註冊新客 zta01–07 | 7 | 網上開戶→補資料→預約；含改密碼、自助取消、寫評價/論壇 |
| 舊客 zc01–04 + 8udc | 5 | 職員建立舊檔；premium/family 升級；12 歲子帳戶全流程 |
| 散客 zd01–10 | 10 | 職員現場代訂；狀態流轉：完成×4／pending／no-show／職員取消／遲到15分／配藥中／保留確認 |
| 高級會員 ze01–05 | 5 | 床位S6、小兒推拿(8歲)、自行改期、取消窗口邊緣測試 |

**最終數據庫狀態**（API 回報與 SQLite 實際記錄一致 ✓）：
32 個測試預約 = confirmed 19 / completed 7 / cancelled 5 / no-show 1
26 個測試用戶、8 條會員訂閱、1 個家庭連結、2 份病歷、1 條進度、1 條回饋、評價/帖子各 1

---

## 二、通過驗證嘅功能（無需修改）

**預約核心** ✅
- 重疊規則生效：同醫師同時段雙訂 → 409
- 過去日期、週日休診、非營業時間（21:00）全部正確拒絕
- 床位服務（S6）容量邏輯正常
- 時段可用性 API 同實際可訂情況一致（30+ 預約零撞單）

**會員分級** ✅
- 訪客只可約 S1 初體驗；越權約 S2 → 403 `membership_required`
- 一般會員升級前只可約 S1 → 正確攔截
- Stripe 未配置時 checkout 正確回錯誤（上線前需配置真實金鑰）

**帳戶安全** ✅
- 重複用戶名／重複電話／弱密碼／無中文名註冊全部被擋
- 錯誤密碼 401＋剩餘次數提示；錯誤驗證碼被拒
- 登出後 token 即時失效（401）；客戶打管理 API → 403；員工想開 admin 帳號 → 403
- 二次驗證（敏感操作需輸入操作者密碼）生效

**診所運作** ✅
- 完整狀態機流轉：pending→confirmed→in-treatment→completed、visited→dispensing、no-show
- 遲到標記（15 分鐘）正常
- 醫師請假 → 當日該醫師時段全封鎖 → 取消請假即恢復
- 家庭子帳戶：開戶(<18)→臨時密碼登入→職員重設→解除連結→重新連結，全鏈路通

**臨床記錄** ✅
- 醫師寫病歷（診斷/治療方案）→ 加進度指標 → AI 分析 → 客人自查病史 → 客人自助記錄感受
- 自助進度缺必填欄位有明確驗證提示

**互動與內容** ✅
- 回饋閉環：提交→管理員回覆→客人未讀數=1→已讀→結案
- 分診問題/AI 問診答題→建議結果全流程通
- 評價提交→管理員審核通過→公開可見；論壇發帖+回覆
- FAQ/公告 CRUD、公開內容讀取（announcements/videos/cases/reviews/forum）

**報表** ✅
- 日/月收入報表、初體驗清單、職員當日預約總覽、密碼重設日誌、可疑活動檢查

---

## 三、發現嘅問題（按嚴重性排序）

### 🔴 高優先

**1. 通知系統「設定」同「實際行為」不符（有金錢風險）**
> ⚠️ 按你指示，通知功能已停止深入測試。以下係測試期間 server.log 自然暴露嘅證據：

- WhatsApp 行緊 **Twilio Sandbox 模式**：每個預約確認都發送失敗（「收件人未加入 Sandbox」）。即係話**而家真客收唔到任何 WhatsApp 確認**。
- WhatsApp 失敗後代碼 fallback 去 **ClickSend 發真實 SMS**——雖然 README 寫「SMS 已全面取消」、通知設定顯示 `SMS=false`，日誌仍顯示每個測試預約都提交咗 ClickSend SMS（Message ID 有紀錄）。
- **建議**：① 即刻檢查 ClickSend 帳戶有冇被扣費；② 移除 SMS fallback 或令 fallback 尊重 `sms_notification_enabled` 開關；③ 用正式 Twilio WhatsApp 號碼取代 Sandbox。

**2. database.db 放喺 OneDrive 同步資料夾 + 兩邊同時作業**
- 實測發生：我測試中途改嘅 admin 密碼，幾分鐘後被另一邊同步覆寫返舊值。
- SQLite 係單一檔案，雲同步係「成隻檔案替換」——兩邊都有寫入嘅話，隨時**成批新預約/會員資料被舊版本蓋走**，而且唔會有任何報錯。
- **建議**：正式營運將資料庫移出 OneDrive 目錄（例如 `C:\clinic-data\`），只同步備份副本唔同步活體檔案；或者規定只有一部機可以寫入。

**3. 客戶取消時間窗存在時區 bug — `routes/bookings.js:884`**
```js
const created = new Date(booking.created_at + (booking.created_at.includes('T') ? '' : 'Z'));
```
`created_at` 存嘅係本地時間字串，但無 'T' 就加 `'Z'` 尾當 UTC 計。香港（UTC+8）伺服器上，「距離建立幾耐」會計多咗 8 小時——即建立後頭 ~8 小時內結果係負數/細過 15 分鐘，**15 分鐘冷靜期檢查形同虛設，客人開單後差唔多 8 小時內可以取消任何 >24h 嘅遠期預約**。

專項驗證結果：
| 案例 | 預期 | 實際 |
|---|---|---|
| A：新預約即刻取消（15分鐘寬限期） | 200 允許 | ✅ 200 |
| B：created_at 回撥 9 小時 + 預約 >24h | 400 cancel_window | ✅ 400 正確阻擋 |
| C：created_at 只回撥 2 小時（<8h 時區偏移） | 400 cancel_window | ❌ **200 取消成功（bug）** |

**修法**：改為 `new Date(booking.created_at.replace(' ', 'T'))`（按本地時間解析），或全站統一存 UTC。
另請留意：而家規則係「預約前 **24 小時內**先可以取消」（錯誤訊息同代碼一致），即係鼓勵臨急取消、反而禁止早啲通知你哋嘅取消——一般診所做法係相反（要 **最少** 24 小時前通知）。請確認業務意圖。

### 🟡 中優先

**4. 職員代訂被「職員自己」嘅會員等級卡住**
- `POST /api/bookings` 用 **token 持有人**嘅 tier 判斷服務檔次。一般級 staff01 代客訂 S2–S6 → 403，錯誤訊息仲叫「客人自己去 Stripe 升級」——櫃檯場景完全行唔通。
- 同時預約嘅 `user_id` 記錄為建立者（職員）而非客人，**報表/收入/病歷歸屬會失真**。
- **建議**：staff/admin 角色跳過 tier 檢查；支援 body 帶 `customerUserId`（限 staff+）把預約歸屬到真實客人帳戶。

**5. `/api/membership/confirm` 嘅 userId fallback 危險**
- `body.userId` 缺失時自動升級「當前登入者」。我測試期間曾因此**意外將 staff01 升級做 premium**（已還原）。建議 userId 必填，缺失就報 400。

**6. 登入速率限制對櫃檯偏緊**
- 20 次/15分鐘/IP。今次測試兩度觸發 429（加上你另一邊同時登入）。一個月 400 客嘅櫃嘅櫃檯，幾個職員共用一個 IP 好易撞牆。
- **建議**：限額改為「帳戶+IP」組合計算，或 staff/doctor 角色用較高上限（例如 60/15min）。

**7. API 路徑不一致**
- FAQ 公開讀取喺 `/api/faqs`（misc router）、其他內容喺 `/api/content/*`、管理喺 `/api/admin/faqs`——三套路徑並存，前端容易出錯（我第一次都中招）。建議統一。

### 🟢 低優先 / 觀察

**8. 子帳戶 `family_head_id` 不一致**：`/family/register` 會寫 child.family_head_id，但 `/family/add` 只寫 family_links 表——功能上靠 links 表查詢所以冇壞，但資料有機會唔一致。
**9. 臨時密碼唔強制更改**：子帳戶用臨時密碼登入後可以直接照用（`must_change_password` 只是提示）。視乎你想唔想強制首登改密。
**10. 電話格式校驗寬鬆**：任何 8 位數字都收（99xxxxxx 假號碼都過）。可加 HK 號碼範圍校驗（6/9 開頭）減少打錯。
**11. 全域限制 500 req/15min/IP**：今次 237 calls 冇事，但前端若加自動輪詢要留意。

---

## 四、功能增強建議（配合月 400 客規模）

| # | 建議 | 價值 | 實現難度 |
|---|---|---|---|
| 1 | **候補名單（waitlist）**：滿格時段可登記候補，有人取消自動 WhatsApp 通知 | 高峯日直接提升填單率 | 中 |
| 2 | **訊息發送狀態面板**：admin 後台顯示每條 WhatsApp/SMS 嘅提交/送達/失敗狀態＋一鍵重發 | 針對 🔴-1，避免「以為通知咗其實冇」 | 低（Twilio callback 已有基礎） |
| 3 | **營運儀表板**：新客vs回頭客比例、醫師佔比、熱門時段熱力圖、月初至今營收曲線 | 數據已有，欠呈現；400客/月好需要 | 中 |
| 4 | **客人有限度自助改期**：確認後、預約前 24h 內准改一次（你而家完全鎖死） | 減輕櫃檯電話量 | 低 |
| 5 | **報表匯出 Excel/PDF**：月尾交數俾股東/會計 | income_import 有入無出 | 低（xlsx 套件已在依賴） |
| 6 | **自動備份排程**：每日定時複製 database.db 到另一磁碟＋保留 14 份 | 配合 🔴-2 | 極低 |
| 7 | **提醒發送日摘要**：cron 每晚彙總「今日發咗幾多提醒、幾多失敗」電郵俾管理員 | 提早發現渠道故障 | 低 |
| 8 | **敏感操作審計日志頁面**：二次驗證事件/刪除用戶/密碼重設集中可查 | 多員工營運必要 | 低（login_attempts/reset_logs 已有底） |
| 9 | **平板優化 staff.html**：櫃檯大按鈕模式（報到/遲到/完成一撳即轉狀態） | 現場效率 | 中 |

## 五、本次未覆蓋蓋範圍（誠實申報）

- 通知批量任務（run-all / 天氣 / 節氣推送）— **按你指示跳過**
- Stripe 真實付款流程（金鑰未配置）
- 病歷音檔/相片 multipart 上傳、頭像上傳、影片上傳、Excel 入賬導入
- 純前端 UI 互動（今次集中 API 層）

---

## 六、測試產物與清理

| 檔案 | 用途 |
|---|---|
| `_e2e_scenario_test.js` | 主場景測試腳本（可重跑） |
| `_e2e_results.json` | 完整逐步結果（181 條記錄） |
| `_tmp_cancel_test.js` | 取消窗口 bug 專項驗證 |
| `_tmp_clean.js` | 清理所有【場景測試】標記數據＋還原 staff01 會員級別 |

所有測試數據均帶「【場景測試】/測試姓名/99xxx電話」標記，與真實客戶完全隔離。
目前殘留：26 個測試用戶、32 個測試預約（D1 有 19 個 confirmed 佔住時段表）、staff01 仍是 premium（清理腳本會還原 general）。
`allow_public_registration` 已還原為 false ✅
