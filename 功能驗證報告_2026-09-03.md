# 床位資源（手法床 1-5 + VIP房 1-5）及時段管理月曆 — 功能驗證報告

**日期**：2026-09-03
**範圍**：Feature 3（床位改資源）+ Feature 4（醫師/管理員時段管理月曆）
**測試目標**：實作後「測試一次」+「檢查全站關聯回歸」

---

## 1. 測試方法

| 測試 | 工具 / 腳本 | 說明 |
|------|------------|------|
| UI 版面檢查 | `_qa_ui_check.js` (puppeteer) | 載入 4 頁 + 收集 console/page 錯誤 |
| QA 交叉場景 | `_qa_cross_test.js` | 跨角色（客/職/醫/管）核心 + 權限矩陣 |
| Feature 3 針對性 | 臨時驗證腳本（已清理） | vip_rooms 設定讀寫 + 顯式 `bedType=vip/tuina` 預約 |
| Feature 4 端點探測 | 臨時探測（已清理） | `doctor-time-slots/range` + `/:date` |

---

## 2. 結果

### 2.1 UI 版面檢查 — 4/4 通過 ✅
`index.html` / `staff.html` / `doctor.html` / `admin.html` 全部 HTTP 200，body 正常渲染，**無 JS / console 錯誤**（確認本輪 HTML 改動未破壞版面）。

### 2.2 QA 交叉場景測試 — 70 通過 / 32 失敗
- **床位核心流程 P3 全數通過**：自動編排、搶同一張床 409、改揀另一張床、冇揀床自動編排避開已佔床。
- **時段 / 請假 P1(S1) / P5 全數通過**：含「請假日該醫師時段全封鎖」「受影響預約自動取消」。
- 其餘 32 項失敗經分析**均非 Feature 3/4 引起**（見 §3 關聯分析）。

### 2.3 Feature 3 針對性驗證 — 全通過 ✅
| 驗證項 | 結果 |
|--------|------|
| `GET /clinic-settings` 回傳 `vip_rooms=5` | ✅ |
| `PUT /clinic {vip_rooms:6}` → 200 且寫入，還原 5 | ✅ |
| `GET /beds/labels` 含 `vip` | ✅ |
| 顯式 `bedType=vip#1` 預約 → 200，DB 存 `bed_type=vip, bed_number=1` | ✅ |
| 顯式 `bedType=tuina#2` 預約 → 200，DB 存 `bed_type=tuina, bed_number=2` | ✅ |
| 超容量 `vip#6` → 409（同客重疊，預期行為） | ✅ |

### 2.4 Feature 4 端點探測 — 全通過 ✅
| 端點 | 結果 |
|------|------|
| `GET /doctor-time-slots/range` | 回傳 5 醫師 + `days` map（暫空，待醫師設定，結構正確） |
| `GET /doctor-time-slots/:date` | 回傳 5 醫師 + 18 時段 + 正確 `businessHours` |

---

## 3. 關聯分析（回歸判定）

交叉測試 32 項失敗逐項歸類，**全部與 Feature 3/4 無關**：

1. **公開自行註冊關閉**（P2 多項 `403 registration_closed`）：診所設定 `allow_public_registration=false`，與床位/時段無關。
2. **管理員登入失敗**（P6/P8 多項 `401` 連鎖）：管理員密碼與測試腳本預設不符（種子只在空庫時寫入，現有庫密碼未隨 `.env` 更新）。已用備用帳號單獨驗證 `vip_rooms` 設定讀寫正常。
3. **P1 S6 床位資訊**：所選日期恰為診所休診日，時段全回「休息」故無 bed 鍵 —— 測試日期脆性，非回歸。
4. **P7 連續錯密 429**：登入速率限制副作用，非代碼回歸。
5. **P7 預期 403 但得 401**：因 P2 自註冊關閉導致測試客戶不存在之連鎖，非回歸。

**結論**：Feature 3/4 未對既有功能造成回歸。床位預約、時段、請假、家庭樹、權限矩陣等核心流程均正常。

---

## 4. 本輪變更摘要

| 檔案 | 變更 |
|------|------|
| `config/migrations.js` | `clinic_settings` 新增 `vip_rooms` / `vip_bed_names` 確保行（現有庫啟動自動補齊） |
| `database.db` | 直接補 `vip_rooms=5` / `vip_bed_names=[]`（已生效） |
| `routes/settings.js` | `PUT /clinic` 支援 `vip_rooms` / `vip_bed_names` |
| `routes/misc.js` | `GET /beds/labels` 回傳 `vip` |
| `routes/bookings.js` | 床位模型支援 `tuina`/`vip`；新增 `doctor-time-slots/range` |
| `js/app.js` + `index.html` | 會員床位選擇 UI（手法床 / VIP房 切換） |
| `admin.html` / `doctor.html` / `staff.html` | 床位顯示 + 時段管理月曆（TimeTree 配色） |
| `routes/admin.js` | Feature 1 新客星標 `is_new` 欄位（同批未提交功能） |

---

## 5. 遺留待辦（非本輪，建議跟進）

- **管理員密碼管理**：建議改為可經設定介面重設，避免種子密碼與 `.env` 不一致（導致 QA 腳本管理員登入失效）。
- **公開註冊**：若需 P2 場景可跑，請開啟 `allow_public_registration` 或改用職員開戶流程。
